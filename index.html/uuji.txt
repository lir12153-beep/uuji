<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>炽炽机 - AI人设模拟器</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary-bg: #ffffff;
            --secondary-bg: #f8f9fa;
            --text-primary: #1a1a1a;
            --text-secondary: #666666;
            --border-color: #e0e0e0;
            --accent-color: #2ecc71;
            --accent-hover: #27ae60;
            --danger-color: #e74c3c;
            --base-font-size: 14px;
            --custom-primary: #2ecc71;
            --custom-secondary: #27ae60;
        }
        body.dark-theme {
            --primary-bg: #1a1a1a;
            --secondary-bg: #2a2a2a;
            --text-primary: #ffffff;
            --text-secondary: #b0b0b0;
            --border-color: #404040;
        }
        body.font-small { --base-font-size: 12px; }
        body.font-medium { --base-font-size: 14px; }
        body.font-large { --base-font-size: 16px; }
        body.font-xlarge { --base-font-size: 18px; }
        * { margin: 0; padding: 0; box-sizing: border-box; -webkit-tap-highlight-color: transparent; }
        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
            font-size: var(--base-font-size);
            background: linear-gradient(135deg, #2ecc71 0%, #27ae60 100%);
            display: flex; justify-content: center; align-items: center;
            min-height: 100vh; overflow: hidden; color: var(--text-primary);
        }
        .draggable-window {
            position: relative; background: var(--secondary-bg);
            border-radius: 0; box-shadow: 0 4px 20px rgba(0, 0, 0, 0.15);
        }
        .window-header {
            background: linear-gradient(135deg, var(--custom-primary) 0%, var(--custom-secondary) 100%);
            padding: 12px 15px; cursor: move; user-select: none;
            display: flex; justify-content: space-between; align-items: center;
        }
        .window-controls { display: flex; align-items: center; gap: 10px; width: 100%; }
        .window-title { color: white; font-weight: 600; font-size: 14px; flex-grow: 1; text-align: center; }
        .window-btn {
            background: rgba(255, 255, 255, 0.2); border: none; color: white;
            width: 28px; height: 28px; border-radius: 4px; cursor: pointer;
            display: flex; align-items: center; justify-content: center; font-size: 16px;
        }
        .window-btn:hover { background: rgba(255, 255, 255, 0.3); }
        .phone-container {
            width: 375px; height: 812px; background: var(--primary-bg);
            overflow: hidden; position: relative; display: flex; flex-direction: column;
        }
        .status-bar {
            height: 44px; background: var(--primary-bg);
            display: flex; justify-content: space-between; align-items: center;
            padding: 0 15px; font-size: 13px; color: var(--text-primary);
            border-bottom: 1px solid var(--border-color); flex-shrink: 0;
        }
        .home-screen {
            flex: 1; background: var(--secondary-bg); padding: 20px 15px;
            overflow-y: auto; display: flex; flex-direction: column; gap: 15px;
        }
        .widget-container {
            background: var(--primary-bg); border-radius: 0; padding: 15px;
            box-shadow: none; border: 1px solid var(--border-color);
        }
        .clock-widget { display: flex; flex-direction: column; align-items: center; justify-content: center; }
        .clock-time { font-size: 48px; font-weight: 300; margin-bottom: 10px; }
        .clock-date { font-size: 16px; color: var(--text-secondary); }
        .app-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 15px; padding: 10px; }
        .app-icon {
            display: flex; flex-direction: column; align-items: center;
            cursor: grab; user-select: none;
        }
        .app-icon-inner {
            width: 60px; height: 60px; border-radius: 0; margin-bottom: 6px;
            box-shadow: none; border: 1px solid var(--border-color);
            display: flex; align-items: center; justify-content: center; font-size: 28px;
            background: linear-gradient(135deg, #2ecc71, #27ae60); color: white;
        }
        .app-icon span { font-size: calc(var(--base-font-size) - 2px); color: var(--text-primary); text-align: center; }
        .dock-bar {
            height: 90px; background: rgba(248, 249, 250, 0.95); backdrop-filter: blur(20px);
            border-top: 1px solid var(--border-color);
            display: flex; justify-content: space-around; align-items: center; padding: 10px 20px;
        }
        body.dark-theme .dock-bar { background: rgba(42, 42, 42, 0.95); }
        .app-container {
            position: absolute; top: 0; left: 0; width: 100%; height: 100%;
            background: var(--primary-bg); z-index: 10; display: none; flex-direction: column;
        }
        .app-container.active { display: flex; }
        .app-header {
            height: 56px; display: flex; align-items: center; justify-content: space-between;
            padding: 0 15px; background: var(--primary-bg); border-bottom: 1px solid var(--border-color);
        }
        .app-title { font-size: calc(var(--base-font-size) + 2px); font-weight: 600; }
        .app-content { flex: 1; overflow-y: auto; }
        .back-btn, .menu-btn {
            background: none; border: none; cursor: pointer; padding: 8px;
            border-radius: 4px; color: var(--text-primary); font-size: 20px;
        }
        .back-btn:hover, .menu-btn:hover { background: var(--secondary-bg); }
        .chat-list { display: flex; flex-direction: column; }
        .chat-item {
            display: flex; padding: 12px 15px; background: var(--primary-bg);
            border-bottom: 1px solid var(--border-color); cursor: pointer;
        }
        .chat-item:hover { background: var(--secondary-bg); }
        .chat-avatar {
            width: 50px; height: 50px; border-radius: 0; margin-right: 12px;
            background: linear-gradient(135deg, #2ecc71, #27ae60);
            display: flex; align-items: center; justify-content: center;
            color: white; font-size: 20px; font-weight: bold; border: 1px solid var(--border-color);
        }
        .chat-info { flex: 1; min-width: 0; }
        .chat-top { display: flex; justify-content: space-between; margin-bottom: 5px; }
        .chat-name { font-weight: 600; }
        .chat-time { font-size: calc(var(--base-font-size) - 2px); color: var(--text-secondary); }
        .chat-preview {
            color: var(--text-secondary); font-size: calc(var(--base-font-size) - 1px);
            overflow: hidden; text-overflow: ellipsis; white-space: nowrap;
        }
        .messages-container { flex: 1; overflow-y: auto; padding: 15px; background: var(--secondary-bg); }
        .message { display: flex; gap: 10px; margin-bottom: 15px; align-items: flex-start; }
        .message.user { flex-direction: row-reverse; }
        .message-avatar {
            width: 40px; height: 40px; border-radius: 0; flex-shrink: 0;
            background: linear-gradient(135deg, #2ecc71, #27ae60);
            display: flex; align-items: center; justify-content: center;
            color: white; font-weight: bold; font-size: 16px; border: 1px solid var(--border-color);
        }
        .message-content {
            max-width: 70%; padding: 10px 15px; border-radius: 4px;
            background: white; word-wrap: break-word; border: 1px solid var(--border-color);
        }
        body.dark-theme .message-content { background: var(--secondary-bg); }
        .message.user .message-content { background: var(--accent-color); color: white; border: none; }
        .message-annotation {
            font-size: calc(var(--base-font-size) - 2px); color: var(--text-secondary);
            font-style: italic; margin-top: 5px; padding-top: 5px;
            border-top: 1px solid rgba(0,0,0,0.1);
        }
        .message.user .message-annotation { border-top-color: rgba(255,255,255,0.2); color: rgba(255,255,255,0.8); }
        .chat-input-area { padding: 15px; background: var(--primary-bg); border-top: 1px solid var(--border-color); }
        .chat-tools { display: flex; gap: 10px; margin-bottom: 10px; flex-wrap: wrap; }
        .tool-btn {
            padding: 6px 12px; border: 1px solid var(--border-color); border-radius: 4px;
            background: var(--secondary-bg); cursor: pointer; font-size: calc(var(--base-font-size) - 1px);
        }
        .tool-btn:hover { background: var(--border-color); }
        .chat-input-row { display: flex; gap: 10px; align-items: flex-end; }
        textarea {
            flex: 1; padding: 10px 15px; border: 1px solid var(--border-color);
            border-radius: 4px; background: var(--secondary-bg); color: var(--text-primary);
            resize: none; font-size: var(--base-font-size); font-family: inherit; max-height: 100px;
        }
        .send-btn, .voice-btn {
            background: var(--accent-color); color: white; border: none;
            padding: 10px 20px; border-radius: 4px; cursor: pointer; font-size: var(--base-font-size);
        }
        .send-btn:hover, .voice-btn:hover { background: var(--accent-hover); }
        .voice-btn {
            width: 40px; height: 40px; padding: 0; border-radius: 4px;
            display: flex; align-items: center; justify-content: center;
        }
        .voice-btn.recording { background: var(--danger-color); animation: pulse 1s infinite; }
        @keyframes pulse { 0%, 100% { transform: scale(1); } 50% { transform: scale(1.1); } }
        .offline-notice {
            background: #fff3cd; color: #856404; padding: 10px 15px; text-align: center;
            font-size: calc(var(--base-font-size) - 1px); border-bottom: 1px solid var(--border-color);
        }
        body.dark-theme .offline-notice { background: #664d03; color: #ffecb5; }
        .modal {
            position: fixed; top: 0; left: 0; width: 100%; height: 100%;
            background: rgba(0, 0, 0, 0.5); display: none; justify-content: center; align-items: center; z-index: 1000;
        }
        .modal.show { display: flex; }
        .modal-content {
            background: var(--primary-bg); border-radius: 0; width: 90%; max-width: 500px;
            max-height: 80vh; overflow: hidden; display: flex; flex-direction: column;
            border: 1px solid var(--border-color);
        }
        .modal-header {
            padding: 20px; border-bottom: 1px solid var(--border-color);
            display: flex; justify-content: space-between; align-items: center;
        }
        .modal-body { padding: 20px; overflow-y: auto; flex: 1; }
        .close-btn { background: none; border: none; font-size: 24px; cursor: pointer; color: var(--text-secondary); }
        .setting-section {
            margin-bottom: 30px; padding-bottom: 20px; border-bottom: 1px solid var(--border-color);
        }
        .setting-section:last-child { border-bottom: none; }
        .setting-section h3 {
            font-size: calc(var(--base-font-size) + 2px); margin-bottom: 15px;
            display: flex; align-items: center; gap: 10px;
        }
        .input-group { margin-bottom: 15px; }
        .input-group label { display: block; margin-bottom: 8px; font-weight: 500; }
        .input-group input, .input-group select, .input-group textarea {
            width: 100%; padding: 10px; border: 1px solid var(--border-color);
            border-radius: 4px; background: var(--secondary-bg); color: var(--text-primary);
            font-size: var(--base-font-size);
        }
        .input-group textarea { resize: vertical; min-height: 80px; font-family: inherit; }
        .color-picker-group { display: flex; gap: 10px; align-items: center; }
        .color-preview { width: 40px; height: 40px; border-radius: 0; border: 2px solid var(--border-color); }
        .action-btn {
            padding: 10px 20px; border: none; border-radius: 4px;
            background: var(--accent-color); color: white; font-size: var(--base-font-size);
            cursor: pointer;
        }
        .action-btn:hover { background: var(--accent-hover); }
        .action-btn.secondary {
            background: var(--secondary-bg); color: var(--text-primary); border: 1px solid var(--border-color);
        }
        .action-btn.danger { background: var(--danger-color); color: white; }
        .button-row { display: flex; gap: 10px; flex-wrap: wrap; }
        .toast-notification {
            position: fixed; top: 20px; left: 50%; transform: translateX(-50%) translateY(-100px);
            background: rgba(0, 0, 0, 0.8); color: white; padding: 12px 20px;
            border-radius: 4px; font-size: var(--base-font-size); z-index: 10000; opacity: 0;
            transition: all 0.3s ease; pointer-events: none;
        }
        .toast-notification.show { transform: translateX(-50%) translateY(0); opacity: 1; }
        .search-box { padding: 15px; background: var(--primary-bg); }
        .search-input {
            width: 100%; padding: 10px 15px; border: 1px solid var(--border-color);
            border-radius: 4px; background: var(--secondary-bg); color: var(--text-primary);
            font-size: var(--base-font-size);
        }
        .api-status {
            display: inline-block; padding: 4px 8px; border-radius: 4px;
            font-size: calc(var(--base-font-size) - 2px); margin-left: 10px;
        }
        .api-status.connected { background: rgba(46, 204, 113, 0.1); color: var(--accent-color); border: 1px solid var(--accent-color); }
        .api-status.disconnected { background: rgba(231, 76, 60, 0.1); color: var(--danger-color); border: 1px solid var(--danger-color); }
        ::-webkit-scrollbar { width: 6px; }
        ::-webkit-scrollbar-thumb { background: rgba(0, 0, 0, 0.2); border-radius: 0; }
        body.dark-theme ::-webkit-scrollbar-thumb { background: rgba(255, 255, 255, 0.2); }
        @media (max-width: 480px) {
            .phone-container { width: 100vw; height: 100vh; }
            .window-header { display: none; }
            .draggable-window { border-radius: 0; width: 100vw; height: 100vh; }
        }
    </style>
</head>
<body class="font-medium">
    <div id="phoneWindowContainer" class="draggable-window">
        <div id="windowHeader" class="window-header">
            <div class="window-controls">
                <button id="minimizeBtn" class="window-btn">−</button>
                <button id="themeToggleBtn" class="window-btn">🌓</button>
                <button id="fontSizeBtn" class="window-btn">A</button>
                <span class="window-title">炽炽机</span>
            </div>
        </div>
        <div class="phone-container">
            <div class="status-bar">
                <span id="currentTime">12:00</span>
                <div><i class="fas fa-wifi"></i> <i class="fas fa-battery-full"></i></div>
            </div>
            <div class="home-screen" id="homeScreen">
                <div class="widget-container clock-widget" style="min-height:180px">
                    <div class="clock-time" id="clockTime">12:00</div>
                    <div class="clock-date" id="clockDate">2026年1月31日</div>
                </div>
                <div class="app-grid" id="appGrid">
                    <div class="app-icon" draggable="true" data-app="qq">
                        <div class="app-icon-inner" style="background: linear-gradient(135deg, #3498db, #2980b9);"><i class="fab fa-qq"></i></div>
                        <span>QQ</span>
                    </div>
                    <div class="app-icon" draggable="true" data-app="wechat">
                        <div class="app-icon-inner" style="background: linear-gradient(135deg, #2ecc71, #27ae60);"><i class="fab fa-weixin"></i></div>
                        <span>微信</span>
                    </div>
                    <div class="app-icon" draggable="true" data-app="settings">
                        <div class="app-icon-inner" style="background: linear-gradient(135deg, #95a5a6, #7f8c8d);"><i class="fas fa-cog"></i></div>
                        <span>设置</span>
                    </div>
                </div>
            </div>
            <div class="dock-bar" id="dockBar">
                <div class="app-icon" draggable="true" data-app="qq">
                    <div class="app-icon-inner" style="background: linear-gradient(135deg, #3498db, #2980b9);"><i class="fab fa-qq"></i></div>
                </div>
                <div class="app-icon" draggable="true" data-app="wechat">
                    <div class="app-icon-inner" style="background: linear-gradient(135deg, #2ecc71, #27ae60);"><i class="fab fa-weixin"></i></div>
                </div>
            </div>

            <!-- QQ App -->
            <div id="qqApp" class="app-container">
                <div class="app-header">
                    <button class="back-btn" onclick="closeApp('qqApp')"><i class="fas fa-chevron-left"></i></button>
                    <span class="app-title">QQ</span>
                    <button class="menu-btn" onclick="createChat('qq')"><i class="fas fa-plus"></i></button>
                </div>
                <div id="qqListPage" style="display: flex; flex-direction: column; height: calc(100% - 56px);">
                    <div class="search-box">
                        <input type="text" class="search-input" placeholder="搜索" oninput="searchChats('qq', this.value)">
                    </div>
                    <div class="chat-list" id="qqList" style="flex: 1; overflow-y: auto;"></div>
                </div>
                <div id="qqChatPage" style="display: none; flex-direction: column; height: calc(100% - 56px);">
                    <div class="app-header" style="border-bottom: 1px solid var(--border-color);">
                        <button class="back-btn" onclick="exitChat('qq')"><i class="fas fa-chevron-left"></i></button>
                        <span class="app-title" id="qqChatTitle">聊天</span>
                        <button class="menu-btn" onclick="openChatSettings('qq')"><i class="fas fa-cog"></i></button>
                    </div>
                    <div class="offline-notice" id="qqOfflineNotice" style="display: none;">📍 线下模式</div>
                    <div class="messages-container" id="qqMessages"></div>
                    <div class="chat-input-area">
                        <div class="chat-tools">
                            <button class="tool-btn" onclick="switchMode('qq')"><i class="fas fa-wifi"></i> <span id="qqModeText">线上</span></button>
                            <button class="tool-btn" onclick="addAnnotation('qq')"><i class="fas fa-comment-dots"></i> 批注</button>
                            <button class="tool-btn" onclick="replyAsCharacter('qq')"><i class="fas fa-user-tag"></i> 代发</button>
                        </div>
                        <div class="chat-input-row">
                            <textarea id="qqInput" rows="1" placeholder="发消息..."></textarea>
                            <button class="send-btn" onclick="sendMessage('qq')">发送</button>
                        </div>
                    </div>
                </div>
            </div>

            <!-- WeChat App -->
            <div id="wechatApp" class="app-container">
                <div class="app-header">
                    <button class="back-btn" onclick="closeApp('wechatApp')"><i class="fas fa-chevron-left"></i></button>
                    <span class="app-title">微信</span>
                    <button class="menu-btn" onclick="createChat('wechat')"><i class="fas fa-plus"></i></button>
                </div>
                <div id="wechatListPage" style="display: flex; flex-direction: column; height: calc(100% - 56px);">
                    <div class="search-box">
                        <input type="text" class="search-input" placeholder="搜索" oninput="searchChats('wechat', this.value)">
                    </div>
                    <div class="chat-list" id="wechatList" style="flex: 1; overflow-y: auto;"></div>
                </div>
                <div id="wechatChatPage" style="display: none; flex-direction: column; height: calc(100% - 56px);">
                    <div class="app-header" style="border-bottom: 1px solid var(--border-color);">
                        <button class="back-btn" onclick="exitChat('wechat')"><i class="fas fa-chevron-left"></i></button>
                        <span class="app-title" id="wechatChatTitle">聊天</span>
                        <button class="menu-btn" onclick="openChatSettings('wechat')"><i class="fas fa-cog"></i></button>
                    </div>
                    <div class="offline-notice" id="wechatOfflineNotice" style="display: none;">📍 线下模式</div>
                    <div class="messages-container" id="wechatMessages"></div>
                    <div class="chat-input-area">
                        <div class="chat-tools">
                            <button class="tool-btn" onclick="switchMode('wechat')"><i class="fas fa-wifi"></i> <span id="wechatModeText">线上</span></button>
                            <button class="tool-btn" onclick="addAnnotation('wechat')"><i class="fas fa-comment-dots"></i> 批注</button>
                        </div>
                        <div class="chat-input-row">
                            <textarea id="wechatInput" rows="1" placeholder="发消息..."></textarea>
                            <button class="send-btn" onclick="sendMessage('wechat')">发送</button>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Settings Modal -->
            <div id="settingsModal" class="modal">
                <div class="modal-content">
                    <div class="modal-header">
                        <h2>设置</h2>
                        <button class="close-btn" onclick="closeSettings()"><i class="fas fa-times"></i></button>
                    </div>
                    <div class="modal-body">
                        <div class="setting-section">
                            <h3><i class="fas fa-robot"></i> AI API 配置 <span id="apiStatusBadge" class="api-status disconnected">未连接</span></h3>
                            <div class="input-group">
                                <label>API Base URL</label>
                                <input type="text" id="apiBaseUrl" placeholder="https://api.anthropic.com" value="https://api.anthropic.com">
                            </div>
                            <div class="input-group">
                                <label>API Key</label>
                                <input type="password" id="apiKey" placeholder="sk-ant-...">
                            </div>
                            <div class="input-group">
                                <label>选择模型</label>
                                <select id="apiModel"><option value="">请先测试连接</option></select>
                            </div>
                            <div class="button-row">
                                <button class="action-btn" onclick="testApiConnection()"><i class="fas fa-plug"></i> 测试连接</button>
                                <button class="action-btn secondary" onclick="saveApiSettings()"><i class="fas fa-save"></i> 保存配置</button>
                            </div>
                        </div>
                        <div class="setting-section">
                            <h3><i class="fas fa-desktop"></i> 显示设置</h3>
                            <div class="input-group">
                                <label>主题模式</label>
                                <select id="themeSelect" onchange="changeTheme(this.value)">
                                    <option value="light">浅色</option>
                                    <option value="dark">深色</option>
                                </select>
                            </div>
                            <div class="input-group">
                                <label>字体大小</label>
                                <select id="fontSizeSelect" onchange="changeFontSize(this.value)">
                                    <option value="small">小</option>
                                    <option value="medium" selected>中</option>
                                    <option value="large">大</option>
                                    <option value="xlarge">特大</option>
                                </select>
                            </div>
                        </div>
                        <div class="setting-section">
                            <h3><i class="fas fa-palette"></i> 颜色自定义</h3>
                            <div class="input-group">
                                <label>主色调</label>
                                <div class="color-picker-group">
                                    <input type="color" id="primaryColorPicker" value="#2ecc71" onchange="updatePrimaryColor(this.value)">
                                    <div class="color-preview" id="primaryColorPreview" style="background: #2ecc71;"></div>
                                    <span style="font-size: 13px;">窗口头部起始色</span>
                                </div>
                            </div>
                            <div class="input-group">
                                <label>辅色调</label>
                                <div class="color-picker-group">
                                    <input type="color" id="secondaryColorPicker" value="#27ae60" onchange="updateSecondaryColor(this.value)">
                                    <div class="color-preview" id="secondaryColorPreview" style="background: #27ae60;"></div>
                                    <span style="font-size: 13px;">窗口头部结束色</span>
                                </div>
                            </div>
                        </div>
                        <div class="setting-section">
                            <h3><i class="fas fa-database"></i> 数据管理</h3>
                            <div class="button-row">
                                <button class="action-btn secondary" onclick="exportData()">导出备份</button>
                                <button class="action-btn secondary" onclick="document.getElementById('importInput').click()">导入备份</button>
                                <button class="action-btn danger" onclick="clearData()">清空数据</button>
                            </div>
                            <input type="file" id="importInput" hidden accept=".json" onchange="importData(event)">
                        </div>
                    </div>
                </div>
            </div>

            <!-- Character Modal -->
            <div id="characterModal" class="modal">
                <div class="modal-content">
                    <div class="modal-header">
                        <h2 id="charModalTitle">创建新联系人</h2>
                        <button class="close-btn" onclick="closeCharacterModal()"><i class="fas fa-times"></i></button>
                    </div>
                    <div class="modal-body">
                        <input type="hidden" id="charEditId">
                        <input type="hidden" id="charPlatform">
                        <div class="input-group">
                            <label>昵称</label>
                            <input type="text" id="charName" placeholder="例如：李泽言">
                        </div>
                        <div class="input-group">
                            <label>头像 (Emoji 或 文字)</label>
                            <input type="text" id="charAvatar" placeholder="例如：👨‍💼 或 李">
                        </div>
                        <div class="setting-section">
                            <h3><i class="fas fa-user-tag"></i> 导入人设</h3>
                            <div class="input-group">
                                <label>人设描述 / 提示词</label>
                                <textarea id="charPersona" placeholder="在这里输入人设详细描述，例如性格、背景故事等...（用于AI生成回复）"></textarea>
                            </div>
                            <div class="input-group">
                                <label>预设自动回复库 (一行一句)</label>
                                <textarea id="charReplies" placeholder="你好呀！&#10;今天想我了吗？&#10;笨蛋，快去睡觉。" style="height: 120px;"></textarea>
                            </div>
                        </div>
                        <div class="button-row" style="justify-content: flex-end;">
                            <button class="action-btn" onclick="saveCharacter()">保存设定</button>
                        </div>
                    </div>
                </div>
            </div>
            <div id="toast" class="toast-notification"></div>
        </div>
    </div>
    <script>
        let qqChats = [], wechatChats = [], currentQQChat = null, currentWechatChat = null;
        let qqMode = 'online', wechatMode = 'online';
        let apiConfig = { baseUrl: 'https://api.anthropic.com', apiKey: '', model: 'claude-sonnet-4-20250514', connected: false };

        function init() {
            loadSettings(); loadAllData(); loadApiConfig(); initDraggable(); updateClock(); renderAll();
            setInterval(updateClock, 1000); showToast('欢迎使用炽炽机！');
        }

        async function testApiConnection() {
            const baseUrl = document.getElementById('apiBaseUrl').value.trim();
            const apiKey = document.getElementById('apiKey').value.trim();
            if (!apiKey) { showToast('请输入 API Key'); return; }
            showToast('正在测试连接...');
            try {
                const response = await fetch(`${baseUrl}/v1/messages`, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json', 'x-api-key': apiKey, 'anthropic-version': '2023-06-01' },
                    body: JSON.stringify({ model: 'claude-sonnet-4-20250514', max_tokens: 10, messages: [{ role: 'user', content: 'Hi' }] })
                });
                if (response.ok) {
                    const models = ['claude-opus-4-5-20251101', 'claude-sonnet-4-5-20250929', 'claude-sonnet-4-20250514', 'claude-haiku-4-5-20251001', 'claude-3-7-sonnet-20250219', 'claude-3-5-sonnet-20241022', 'claude-3-5-haiku-20241022'];
                    const modelSelect = document.getElementById('apiModel');
                    modelSelect.innerHTML = models.map(m => `<option value="${m}">${m}</option>`).join('');
                    document.getElementById('apiStatusBadge').textContent = '已连接';
                    document.getElementById('apiStatusBadge').className = 'api-status connected';
                    apiConfig.connected = true; showToast('✅ API 连接成功！');
                } else {
                    const error = await response.json();
                    showToast(`❌ 连接失败: ${error.error?.message || '未知错误'}`);
                    document.getElementById('apiStatusBadge').textContent = '连接失败';
                    document.getElementById('apiStatusBadge').className = 'api-status disconnected';
                }
            } catch (error) {
                showToast(`❌ 网络错误: ${error.message}`);
                document.getElementById('apiStatusBadge').textContent = '网络错误';
                document.getElementById('apiStatusBadge').className = 'api-status disconnected';
            }
        }

        function saveApiSettings() {
            apiConfig.baseUrl = document.getElementById('apiBaseUrl').value.trim();
            apiConfig.apiKey = document.getElementById('apiKey').value.trim();
            apiConfig.model = document.getElementById('apiModel').value;
            localStorage.setItem('apiConfig', JSON.stringify(apiConfig));
            showToast('API 配置已保存');
        }

        function loadApiConfig() {
            const saved = localStorage.getItem('apiConfig');
            if (saved) {
                apiConfig = JSON.parse(saved);
                document.getElementById('apiBaseUrl').value = apiConfig.baseUrl;
                document.getElementById('apiKey').value = apiConfig.apiKey;
                if (apiConfig.connected) {
                    document.getElementById('apiStatusBadge').textContent = '已保存';
                    document.getElementById('apiStatusBadge').className = 'api-status connected';
                }
            }
        }

        async function getAIReply(chat, userMessage) {
            if (!apiConfig.apiKey || !apiConfig.connected) return getDefaultReply(chat);
            try {
                const systemPrompt = chat.persona ? `你是${chat.name}。人设：${chat.persona}\n请保持角色，用第一人称回复。` : `你是${chat.name}，请用友好的方式回复。`;
                const response = await fetch(`${apiConfig.baseUrl}/v1/messages`, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json', 'x-api-key': apiConfig.apiKey, 'anthropic-version': '2023-06-01' },
                    body: JSON.stringify({ model: apiConfig.model, max_tokens: 500, system: systemPrompt, messages: [{ role: 'user', content: userMessage }] })
                });
                if (response.ok) {
                    const data = await response.json();
                    return data.content[0].text;
                } else { return getDefaultReply(chat); }
            } catch (error) { return getDefaultReply(chat); }
        }

        function getDefaultReply(chat) {
            if (chat.customReplies && chat.customReplies.length > 0) {
                return chat.customReplies[Math.floor(Math.random() * chat.customReplies.length)];
            }
            const defaultReplies = ['收到！', '好的呢~', '明白了', '嗯嗯！', '有趣', '怎么了？'];
            return defaultReplies[Math.floor(Math.random() * defaultReplies.length)];
        }

        function updateClock() {
            const now = new Date();
            const time = now.toLocaleTimeString('zh-CN', { hour: '2-digit', minute: '2-digit' });
            const date = now.toLocaleDateString('zh-CN', { year: 'numeric', month: 'long', day: 'numeric', weekday: 'long' });
            document.getElementById('currentTime').textContent = time;
            document.getElementById('clockTime').textContent = time;
            document.getElementById('clockDate').textContent = date;
        }

        function initDraggable() {
            const container = document.getElementById('phoneWindowContainer');
            const header = document.getElementById('windowHeader');
            let isDragging = false, currentX, currentY, initialX, initialY;
            header.addEventListener('mousedown', (e) => {
                if (e.target.closest('.window-btn')) return;
                isDragging = true;
                initialX = e.clientX - (container.offsetLeft || 0);
                initialY = e.clientY - (container.offsetTop || 0);
            });
            document.addEventListener('mousemove', (e) => {
                if (!isDragging) return;
                e.preventDefault();
                currentX = e.clientX - initialX; currentY = e.clientY - initialY;
                container.style.position = 'fixed';
                container.style.left = currentX + 'px'; container.style.top = currentY + 'px';
            });
            document.addEventListener('mouseup', () => isDragging = false);
        }

        document.getElementById('minimizeBtn').onclick = () => {
            const container = document.getElementById('phoneWindowContainer');
            container.style.display = container.style.display === 'none' ? 'block' : 'none';
            showToast(container.style.display === 'none' ? '窗口已最小化' : '窗口已恢复');
        };

        document.getElementById('themeToggleBtn').onclick = () => {
            document.body.classList.toggle('dark-theme');
            const isDark = document.body.classList.contains('dark-theme');
            localStorage.setItem('theme', isDark ? 'dark' : 'light');
            showToast(`已切换到${isDark ? '深色' : '浅色'}主题`);
        };

        document.getElementById('fontSizeBtn').onclick = () => {
            const sizes = ['small', 'medium', 'large', 'xlarge'];
            const current = [...document.body.classList].find(c => c.startsWith('font-'))?.replace('font-', '') || 'medium';
            const nextIndex = (sizes.indexOf(current) + 1) % sizes.length;
            document.body.className = document.body.className.replace(/font-\w+/g, '');
            document.body.classList.add(`font-${sizes[nextIndex]}`);
            localStorage.setItem('fontSize', sizes[nextIndex]);
            showToast(`字体：${{ small: '小', medium: '中', large: '大', xlarge: '特大' }[sizes[nextIndex]]}`);
        };

        function updatePrimaryColor(color) {
            document.documentElement.style.setProperty('--custom-primary', color);
            document.getElementById('primaryColorPreview').style.background = color;
            localStorage.setItem('customPrimaryColor', color);
        }

        function updateSecondaryColor(color) {
            document.documentElement.style.setProperty('--custom-secondary', color);
            document.getElementById('secondaryColorPreview').style.background = color;
            localStorage.setItem('customSecondaryColor', color);
        }

        document.querySelectorAll('.app-icon').forEach(icon => {
            icon.addEventListener('click', function() {
                const app = this.dataset.app;
                if (!app) return;
                if (app === 'settings') { openSettings(); }
                else {
                    const appMap = { qq: 'qqApp', wechat: 'wechatApp' };
                    if (appMap[app]) {
                        document.getElementById('homeScreen').style.display = 'none';
                        document.getElementById(appMap[app]).classList.add('active');
                    }
                }
            });
        });

        function closeApp(appId) {
            document.getElementById(appId).classList.remove('active');
            document.getElementById('homeScreen').style.display = 'flex';
        }

        function createChat(platform) {
            document.getElementById('charModalTitle').textContent = '创建新联系人';
            document.getElementById('charEditId').value = '';
            document.getElementById('charPlatform').value = platform;
            document.getElementById('charName').value = '';
            document.getElementById('charAvatar').value = '';
            document.getElementById('charPersona').value = '';
            document.getElementById('charReplies').value = '';
            document.getElementById('characterModal').classList.add('show');
        }

        function openChatSettings(platform) {
            const chat = platform === 'qq' ? currentQQChat : currentWechatChat;
            if (!chat) return;
            document.getElementById('charModalTitle').textContent = '聊天设置 (人设)';
            document.getElementById('charEditId').value = chat.id;
            document.getElementById('charPlatform').value = platform;
            document.getElementById('charName').value = chat.name;
            document.getElementById('charAvatar').value = chat.avatar;
            document.getElementById('charPersona').value = chat.persona || '';
            document.getElementById('charReplies').value = (chat.customReplies || []).join('\n');
            document.getElementById('characterModal').classList.add('show');
        }

        function closeCharacterModal() { document.getElementById('characterModal').classList.remove('show'); }

        function saveCharacter() {
            const platform = document.getElementById('charPlatform').value;
            const editId = document.getElementById('charEditId').value;
            const name = document.getElementById('charName').value.trim();
            const avatar = document.getElementById('charAvatar').value.trim() || name[0];
            const persona = document.getElementById('charPersona').value;
            const repliesText = document.getElementById('charReplies').value.trim();
            const customReplies = repliesText ? repliesText.split('\n').map(l => l.trim()).filter(l => l.length > 0) : [];
            if (!name) { showToast('请输入昵称'); return; }

            if (editId) {
                const chats = platform === 'qq' ? qqChats : wechatChats;
                const chat = chats.find(c => c.id == editId);
                if (chat) {
                    chat.name = name; chat.avatar = avatar; chat.persona = persona; chat.customReplies = customReplies;
                    if(platform === 'qq') document.getElementById('qqChatTitle').textContent = name;
                    else document.getElementById('wechatChatTitle').textContent = name;
                    showToast('设置已更新');
                }
            } else {
                const chat = { id: Date.now(), name, avatar, persona, customReplies, messages: [], lastMessage: '', lastTime: Date.now() };
                if (platform === 'qq') { qqChats.push(chat); renderQQList(); }
                else { wechatChats.push(chat); renderWechatList(); }
                showToast(`已添加 ${name}`);
            }
            if (platform === 'qq') saveQQChats(); else saveWechatChats();
            closeCharacterModal();
        }

        function searchChats(platform, keyword) {
            const chats = platform === 'qq' ? qqChats : wechatChats;
            const filtered = chats.filter(c => c.name.toLowerCase().includes(keyword.toLowerCase()));
            if (platform === 'qq') renderQQList(filtered); else renderWechatList(filtered);
        }

        function renderQQList(list = qqChats) {
            const container = document.getElementById('qqList');
            if (list.length === 0) { container.innerHTML = '<div style="text-align:center;padding:40px;color:var(--text-secondary);">暂无聊天</div>'; return; }
            container.innerHTML = list.map(chat => `
                <div class="chat-item" onclick="openChat('qq', ${chat.id})">
                    <div class="chat-avatar">${chat.avatar}</div>
                    <div class="chat-info">
                        <div class="chat-top">
                            <div class="chat-name">${escapeHtml(chat.name)}</div>
                            <div class="chat-time">${formatTime(chat.lastTime)}</div>
                        </div>
                        <div class="chat-preview">${escapeHtml(chat.lastMessage || '点击开始聊天')}</div>
                    </div>
                </div>
            `).join('');
        }

        function renderWechatList(list = wechatChats) {
            const container = document.getElementById('wechatList');
            if (list.length === 0) { container.innerHTML = '<div style="text-align:center;padding:40px;color:var(--text-secondary);">暂无聊天</div>'; return; }
            container.innerHTML = list.map(chat => `
                <div class="chat-item" onclick="openChat('wechat', ${chat.id})">
                    <div class="chat-avatar">${chat.avatar}</div>
                    <div class="chat-info">
                        <div class="chat-top">
                            <div class="chat-name">${escapeHtml(chat.name)}</div>
                            <div class="chat-time">${formatTime(chat.lastTime)}</div>
                        </div>
                        <div class="chat-preview">${escapeHtml(chat.lastMessage || '点击开始聊天')}</div>
                    </div>
                </div>
            `).join('');
        }

        function openChat(platform, chatId) {
            if (platform === 'qq') {
                currentQQChat = qqChats.find(c => c.id === chatId);
                document.getElementById('qqListPage').style.display = 'none';
                document.getElementById('qqChatPage').style.display = 'flex';
                document.getElementById('qqChatTitle').textContent = currentQQChat.name;
                document.getElementById('qqOfflineNotice').style.display = qqMode === 'offline' ? 'block' : 'none';
                renderMessages('qq');
            } else {
                currentWechatChat = wechatChats.find(c => c.id === chatId);
                document.getElementById('wechatListPage').style.display = 'none';
                document.getElementById('wechatChatPage').style.display = 'flex';
                document.getElementById('wechatChatTitle').textContent = currentWechatChat.name;
                document.getElementById('wechatOfflineNotice').style.display = wechatMode === 'offline' ? 'block' : 'none';
                renderMessages('wechat');
            }
        }

        function exitChat(platform) {
            if (platform === 'qq') {
                document.getElementById('qqChatPage').style.display = 'none';
                document.getElementById('qqListPage').style.display = 'flex';
                renderQQList(); currentQQChat = null;
            } else {
                document.getElementById('wechatChatPage').style.display = 'none';
                document.getElementById('wechatListPage').style.display = 'flex';
                renderWechatList(); currentWechatChat = null;
            }
        }

        function renderMessages(platform) {
            const chat = platform === 'qq' ? currentQQChat : currentWechatChat;
            const container = document.getElementById(platform === 'qq' ? 'qqMessages' : 'wechatMessages');
            if (!chat || !chat.messages) { container.innerHTML = ''; return; }
            container.innerHTML = chat.messages.map(msg => `
                <div class="message ${msg.sender}">
                    <div class="message-avatar">${msg.sender === 'user' ? 'Me' : chat.avatar}</div>
                    <div class="message-content">
                        ${escapeHtml(msg.content)}
                        ${msg.annotation ? `<div class="message-annotation">${escapeHtml(msg.annotation)}</div>` : ''}
                    </div>
                </div>
            `).join('');
            container.scrollTop = container.scrollHeight;
        }

        async function sendMessage(platform) {
            const input = document.getElementById(platform === 'qq' ? 'qqInput' : 'wechatInput');
            const content = input.value.trim();
            if (!content) return;
            const chat = platform === 'qq' ? currentQQChat : currentWechatChat;
            const mode = platform === 'qq' ? qqMode : wechatMode;
            chat.messages.push({ sender: 'user', content: content, timestamp: Date.now(), annotation: '' });
            chat.lastMessage = content; chat.lastTime = Date.now();
            input.value = '';
            if (platform === 'qq') saveQQChats(); else saveWechatChats();
            renderMessages(platform);

            setTimeout(async () => {
                let reply;
                if (mode === 'offline') { reply = `【线下】${chat.name} 看到消息了，正在思考...`; }
                else { reply = await getAIReply(chat, content); }
                chat.messages.push({ sender: 'ai', content: reply, timestamp: Date.now(), annotation: '' });
                chat.lastMessage = reply; chat.lastTime = Date.now();
                if (platform === 'qq') saveQQChats(); else saveWechatChats();
                renderMessages(platform);
            }, 1000);
        }

        function replyAsCharacter(platform) {
            const reply = prompt("请输入你想让TA回复的内容：");
            if (!reply) return;
            const chat = platform === 'qq' ? currentQQChat : currentWechatChat;
            chat.messages.push({ sender: 'ai', content: reply, timestamp: Date.now(), annotation: '' });
            chat.lastMessage = reply; chat.lastTime = Date.now();
            if (platform === 'qq') saveQQChats(); else saveWechatChats();
            renderMessages(platform);
        }

        function switchMode(platform) {
            if (platform === 'qq') {
                qqMode = qqMode === 'online' ? 'offline' : 'online';
                document.getElementById('qqModeText').textContent = qqMode === 'online' ? '线上' : '线下';
                document.getElementById('qqOfflineNotice').style.display = qqMode === 'offline' ? 'block' : 'none';
                showToast(`QQ已切换到${qqMode === 'online' ? '线上' : '线下'}模式`);
            } else {
                wechatMode = wechatMode === 'online' ? 'offline' : 'online';
                document.getElementById('wechatModeText').textContent = wechatMode === 'online' ? '线上' : '线下';
                document.getElementById('wechatOfflineNotice').style.display = wechatMode === 'offline' ? 'block' : 'none';
                showToast(`微信已切换到${wechatMode === 'online' ? '线上' : '线下'}模式`);
            }
        }

        function addAnnotation(platform) {
            const annotation = prompt('输入批注:');
            if (!annotation) return;
            const chat = platform === 'qq' ? currentQQChat : currentWechatChat;
            if (chat && chat.messages.length > 0) {
                chat.messages[chat.messages.length - 1].annotation = annotation;
                if (platform === 'qq') saveQQChats(); else saveWechatChats();
                renderMessages(platform); showToast('批注已添加');
            }
        }

        function openSettings() { document.getElementById('settingsModal').classList.add('show'); }
        function closeSettings() { document.getElementById('settingsModal').classList.remove('show'); }

        function changeTheme(theme) {
            document.body.classList.toggle('dark-theme', theme === 'dark');
            localStorage.setItem('theme', theme);
        }

        function changeFontSize(size) {
            document.body.className = document.body.className.replace(/font-\w+/g, '');
            document.body.classList.add(`font-${size}`);
            localStorage.setItem('fontSize', size);
        }

        function exportData() {
            const data = { version: '5.0.0', exportTime: new Date().toISOString(), qqChats, wechatChats, theme: localStorage.getItem('theme'), fontSize: localStorage.getItem('fontSize'), customPrimaryColor: localStorage.getItem('customPrimaryColor'), customSecondaryColor: localStorage.getItem('customSecondaryColor'), apiConfig };
            const blob = new Blob([JSON.stringify(data, null, 2)], { type: 'application/json' });
            const url = URL.createObjectURL(blob);
            const a = document.createElement('a');
            a.href = url; a.download = `chichi-backup-${Date.now()}.json`;
            a.click(); URL.revokeObjectURL(url);
            showToast('备份已导出');
        }

        function importData(e) {
            const file = e.target.files[0];
            if (!file) return;
            const reader = new FileReader();
            reader.onload = (e) => {
                try {
                    const data = JSON.parse(e.target.result);
                    if (data.qqChats) qqChats = data.qqChats;
                    if (data.wechatChats) wechatChats = data.wechatChats;
                    saveAllData();
                    showToast('备份已恢复，即将刷新...'); setTimeout(() => location.reload(), 1500);
                } catch { showToast('备份文件格式错误'); }
            };
            reader.readAsText(file);
        }

        function clearData() {
            if (confirm('确定清空所有数据？')) {
                localStorage.clear(); showToast('已清空'); setTimeout(() => location.reload(), 1000);
            }
        }

        function saveQQChats() { localStorage.setItem('qqChats', JSON.stringify(qqChats)); }
        function saveWechatChats() { localStorage.setItem('wechatChats', JSON.stringify(wechatChats)); }
        function saveAllData() { saveQQChats(); saveWechatChats(); }
        function loadAllData() {
            qqChats = JSON.parse(localStorage.getItem('qqChats') || '[]');
            wechatChats = JSON.parse(localStorage.getItem('wechatChats') || '[]');
        }

        function loadSettings() {
            const theme = localStorage.getItem('theme');
            if (theme === 'dark') document.body.classList.add('dark-theme');
            const fontSize = localStorage.getItem('fontSize') || 'medium';
            document.body.classList.add(`font-${fontSize}`);
            const primary = localStorage.getItem('customPrimaryColor');
            if (primary) {
                document.documentElement.style.setProperty('--custom-primary', primary);
                document.getElementById('primaryColorPicker').value = primary;
                document.getElementById('primaryColorPreview').style.background = primary;
            }
            const secondary = localStorage.getItem('customSecondaryColor');
            if (secondary) {
                document.documentElement.style.setProperty('--custom-secondary', secondary);
                document.getElementById('secondaryColorPicker').value = secondary;
                document.getElementById('secondaryColorPreview').style.background = secondary;
            }
        }

        function renderAll() { renderQQList(); renderWechatList(); }

        function showToast(msg, duration = 2000) {
            const toast = document.getElementById('toast');
            toast.textContent = msg; toast.classList.add('show');
            setTimeout(() => toast.classList.remove('show'), duration);
        }

        function escapeHtml(text) {
            if (!text) return '';
            const div = document.createElement('div');
            div.textContent = text;
            return div.innerHTML.replace(/\n/g, '<br>');
        }

        function formatTime(timestamp) {
            const date = new Date(timestamp), now = new Date(), diff = now - date;
            if (diff < 60000) return '刚刚';
            if (diff < 3600000) return `${Math.floor(diff / 60000)}分钟前`;
            if (diff < 86400000) return date.toLocaleTimeString('zh-CN', { hour: '2-digit', minute: '2-digit' });
            return date.toLocaleDateString('zh-CN', { month: 'numeric', day: 'numeric' });
        }

        document.addEventListener('DOMContentLoaded', init);
    </script>
</body>
</html>